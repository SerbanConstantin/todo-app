<div class="card">
  <div class="card-header">
    <h4>Add New List</h4>
</div>

<div class="card-body">
    <%= render 'form', list: @list %>
</div>
  </div>

<br/>
<%= link_to 'Back', lists_path, class:"btn btn-warning" %>
